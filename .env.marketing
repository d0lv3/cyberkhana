# Loaded by `vite --mode marketing`. No secrets — this file is committed on
# purpose, so `npm run build:marketing` produces the cyberkhana.tech build on
# any machine without anyone having to remember an environment variable.
VITE_SITE=marketing
